# Roma-STT

Локальное распознавание речи в текст (Speech-to-Text) для Windows: приложение в трее, глобальная горячая клавиша, движок [whisper.cpp](https://github.com/ggml-org/whisper.cpp) (CPU / CUDA / AMD). **Только для Windows.**

Мой друг Рома сломал правую руку, поэтому я написал это приложение — чтобы он мог набирать текст голосом: без лишних телодвижений, одним сочетанием клавиш (запись → стоп → текст вставляется в месте курсора).

**Подробная инструкция для пользователя:** см. [ИНСТРУКЦИЯ.md](ИНСТРУКЦИЯ.md).

## Необходимые программы

Нужны только один раз для подготовки среды.  
**Самый простой способ:** запустите **`roma-stt.bat`** **от имени администратора** (правый щелчок → «Запуск от имени администратора») и выберите **0** (Установить программы). Скрипт проверит наличие **WinGet**, при необходимости установит его, а затем через него поставит uv, Git, CMake и Visual Studio Build Tools. Если программы уже есть, они будут пропущены.

**Если WinGet не установлен** (и скрипт не смог его поставить), выполните в PowerShell от имени администратора:

```powershell
$progressPreference = 'silentlyContinue'
Install-PackageProvider -Name NuGet -Force | Out-Null
Install-Module -Name Microsoft.WinGet.Client -Force -Repository PSGallery | Out-Null
Repair-WinGetPackageManager -AllUsers
```

После этого перезапустите батник от имени администратора и снова выберите пункт **0**. Ссылки на ручную установку — в таблице ниже.

| Программа | Назначение | Команда (winget) | Ссылка |
|-----------|------------|---------------------|--------|
| **uv** | Среда Python и зависимости | `winget install astral-sh.uv` | [astral.sh/uv](https://docs.astral.sh/uv/getting-started/installation/) |
| **Git** | Скачивание whisper.cpp | `winget install Git.Git` | [git-scm.com](https://git-scm.com/download/win) |
| **CMake** | Сборка whisper.cpp | `winget install Kitware.CMake` | [cmake.org](https://cmake.org/download/) |
| **VS Build Tools** | Компилятор C++ | `winget install Microsoft.VisualStudio.2022.BuildTools --override "--wait --passive --add Microsoft.VisualStudio.Workload.VCTools"` | [visualstudio.microsoft.com](https://visualstudio.microsoft.com/visual-cpp-build-tools/) |

## Установка и запуск

1. Выполните пункт **0** в меню (если не ставили программы выше).
2. Выберите пункт **1** (Установка). Скрипт создаст виртуальную среду, установит зависимости, **скачает/соберёт whisper.cpp под вашу архитектуру (CPU/GPU)** и подготовит модель.

### Варианты запуска

- **Интерактивный (меню):** запустите **`roma-stt.bat`** без аргументов. Появится меню, рекомендуемый порядок: 0 → 1 → 2 → 3.
- **Командная строка:** **`roma-stt.bat <команда> [аргументы]`**:
  - `install-tools` — установка системных программ (как пункт 0).
  - `install [--arch cpu|cuda|amd]` — полная установка (venv, сборка whisper, модель).
  - `check` — проверка готовности.
  - `download <имя>` — скачать модель (например, `download small`).
  - `build-whisper [--arch cpu|cuda|amd]` — только пересборка whisper.cpp.
  - `start [cpu|cuda|amd]` — запуск приложения в трее.
  - `stop` — остановка приложения.

### Пункты меню (порядок: установка → запуск → настройки → выход)

- **0** — Установить системные программы (через WinGet).
- **1** — Установить Roma-STT (среда, сборка whisper.cpp, модель large-v3-turbo по умолчанию).
- **2** — Проверка готовности системы.
- **3** — Запустить программу (сворачивается в трей).
- **4** — Остановить программу.
- **5** — Модели распознавания (список, выбор или скачать и выбрать).
- **6** — Подбор свободных горячих клавиш (запись и стоп).
- **7** — Горячая клавиша записи (по умолчанию `Ctrl+F2`).
- **8** — Горячая клавиша стопа (по умолчанию `Ctrl+F3`).
- **9** — Выбрать язык распознавания (по умолчанию `ru`).
- **10** — Устройство ввода (микрофон): сканировать и выбрать номер.
- **11** — Удалить установку (очистить `.venv` и `models/`).
- **12** — Выход.

## Конфигурация (`config.yaml`)

- `module`: текущий режим работы (`cpu`, `cuda` или `amd`).
- `hotkey_record`: клавиша записи (по умолчанию `Ctrl+F2`).
- `hotkey_stop`: клавиша стопа (по умолчанию `Ctrl+F3`).
- `whisper_cpp_path_*`: пути к исполняемым файлам whisper.cpp.
- `whisper_model_path`: путь к мультиязычной модели.
- `language`: язык распознавания (ru, en и др.).
- `input_device`: индекс микрофона (пункт 10 — сканировать устройства); не задан — системный по умолчанию.

## whisper.cpp и GPU

При установке (пункт **1**) или пересборке (`build-whisper`) программа спрашивает тип видеокарты.  

- **CUDA** (NVIDIA): требуется [NVIDIA CUDA Toolkit](https://developer.nvidia.com/cuda-downloads). Пункт **0** автоматически определяет видеокарту NVIDIA и предлагает поставить CUDA Toolkit (`winget install -e --id Nvidia.CUDA`). После сборки, если cmake не находит nvcc, задайте `CUDAToolkit_ROOT` (путь к папке CUDA, например `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.x`).
- **AMD** (Vulkan): требуется [Vulkan SDK](https://vulkan.lunarg.com/). Пункт **0** определяет видеокарту AMD и предлагает поставить Vulkan SDK (`winget install --id KhronosGroup.VulkanSDK`).
- GPU-ускорение включается на уровне компиляции whisper.cpp (флаги `-DGGML_CUDA=ON` / `-DGGML_VULKAN=ON`). Если выбрана архитектура cuda или amd, движок также передаёт `-ngl 99` при распознавании; если бинарник не поддерживает этот флаг — автоматически повторяет запуск без него.

## Использование

1. Запустите программу (пункт **3**). Она появится в трее рядом с часами.
2. Нажмите клавишу записи (по умолчанию **Ctrl+F2**), говорите, затем нажмите клавишу стопа (**Ctrl+F3**):
   - **Один сигнал:** началась запись.
   - **Два сигнала:** запись окончена, текст распознаётся и вставляется в место курсора.

## Разработка и тесты

```bash
uv run pytest tests/ -v
```
